/**
 * Chess board layout constants
 */

/**
 * Files (columns) on a chess board
 */
export const FILES = ['a', 'b', 'c', 'd', 'e', 'f', 'g', 'h'];

/**
 * Ranks (rows) on a chess board
 */
export const RANKS = [8, 7, 6, 5, 4, 3, 2, 1];
